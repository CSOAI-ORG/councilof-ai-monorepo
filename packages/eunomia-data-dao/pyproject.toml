[project]
name = "eunomia-data-dao"
version = "0.1.0"
description = "The data-DAO axis bridge (Bridge 5). Governs data commons membership, rights, and the data-dao measurement axis."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
